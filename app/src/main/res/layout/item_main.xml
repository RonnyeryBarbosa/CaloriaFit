<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:orientation="horizontal"
            android:gravity="center"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="H,1:1">


        <ImageView
                android:id="@+id/item_main_img"
                android:layout_width="@dimen/main_img_size"
                android:layout_height="@dimen/main_img_size"

                android:src="@drawable/ic_announcement_black"
                android:layout_marginEnd="10dp"
                tools:ignore="ContentDescription"/>

        <TextView
                android:id="@+id/item_mais_text"
                android:layout_width="@dimen/main_size_font"
                android:layout_height="@dimen/main_size_font"
                android:text="@string/imc"/>


    </LinearLayout>

</android.support.constraint.ConstraintLayout>